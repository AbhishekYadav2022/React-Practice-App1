import React from 'react'

function ArrayComponent() {
    // const myArray = ['Apple', 'Banana', 'Orange']
    // const myList = myArray.map((item) => <p>{item}</p>)
  return (
    <div>
        {/* <h2>These are the items of the array.</h2>
        {myList} */}
    </div>
  )
}

export default ArrayComponent